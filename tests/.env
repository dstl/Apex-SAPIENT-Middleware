# Elastic search version
STACK_VERSION=8.7.1
# Name of the cluster
CLUSTER_NAME=apex-es
# A password for accessing elastic search
ELASTIC_PASSWORD=changeme
# Port of elastic search
ES_PORT=9200
# Port of kibana
KIBANA_PORT=5601
# Memory limit for the elasticsearch container (bytes)
ES_MEM_LIMIT=2147483648
# Memory limit for the kibana container (bytes)
KB_MEM_LIMIT=1073741824
# A password for accessing kibana
KIBANA_PASSWORD=elastic
# Relative or absolute path to output directory for authentication
# Note that an empty directory is setup for convenience: ./tests/credentials
CREDENTIALS_DIR=./credentials
# Root repo directory - only in docker-compose.test.yml
ROOT_REPO_DIR=../
# Test outputs - only in docker-compose.test.yml
TEST_OUTPUT_DIR=.
# SAMPLE Predefined Key only to be used in POC environments
ENCRYPTION_KEY=c34d38b3a14956121ff2170e5030b471551370178f43e5626eec58b04a30fae2
